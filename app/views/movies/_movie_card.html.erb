<div class='card'>
  <%= render partial: 'bookmarks', locals: { movie: movie } %>
  <%= image_tag movie.poster_url, class: 'card-img' %>
  <div class="card-body">
    <div class='card-info'>
      <p><%= movie.rating %>⭐</p>
      <p><a href="<%= movie_path(movie) %>"><%= movie.title %></a></p>
    </div>
  </div>
</div>
