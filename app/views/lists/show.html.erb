<div class='container'>
<% @list.bookmarks.each do |bookmark| %>
  <h1><%= @list.name %></h1>
  <p><%= link_to 'Delete bookmark', bookmark_path(bookmark), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></p>
<% end %>

<p><%= link_to 'Back to lists', lists_path %></p>
</div>
