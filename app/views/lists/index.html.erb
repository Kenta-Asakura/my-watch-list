<div class='index-container'>
  <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://kokuwa-ds.jp/wp-content/uploads/star_theaters_banner.jpg);">
    <h1>Welcome to this lame *ss page</h1>
  </div>

  <h1 class='header'>Categories</h1>
  <div class='cards'>
    <div data-controller='search-movies'>
    <%= form_with url: lists_path, method: :get, html: {class:
      "mb-4", data: {search_movies_target: "form"}} do |f| %>
      <%= f.text_field :query,
                     class: "form-control",
                     placeholder: "Type a movie title",
                     value: params[:query],
                     data: {search_movies_target: "input"} %>
                     <% end %>
    </div>
    <% Movie.all.each do |movie| %>
      <%= movie.title %>
    <% end %>
    <%# <% @lists.each do |list| %>
      <%# <div class='card'> %>
        <%# <%= cl_image_tag list.photo.key, class: 'card-img' %>
      <%# <div class="card-body"> %>
        <%# <%= link_to list.name, list_path(list) %>
      <%# </div> %>
    <%# </div> %>
    <%# <% end %>
  </div>
</div>
